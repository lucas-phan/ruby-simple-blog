<h1><%= t("create_post") %></h1>
<% if @post.errors.any? %>
    <% @post.errors.full_messages.each do |msg| %>
        <%= msg %>
    <% end %>
<% end %>
<%= form_for :post, url: posts_path do |f| %>
    <p>
        <%= f.label t("title") %><br/>
        <%= f.text_field(:title, {:class => 'form-control'}) %>

    </p>
    <p>
        <%= f.label t("body") %>
        <%= f.text_area(:body, {:class => 'form-control'}) %>
    </p>
    <p>
        <%= f.submit(t("create_post"), {:class => 'btn btn-success'}) %>
    </p>
<% end %>
