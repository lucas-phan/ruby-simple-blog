<h1><%= t('simple_blog') %>
<h2><%= t('hello') %></h2>
<% @posts.each do |post| %>
    <div class="well">
        <h3><%= post.title %></h3>
        <p><%= post.body %> </p>
        <%= link_to t('read_more'), post_path(post), :class => "btn btn-primary" %>
    </div>
<% end %>
